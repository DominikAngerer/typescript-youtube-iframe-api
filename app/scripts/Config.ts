// LIVE
export let config = {
}

// TEST
if (window.location.hostname.search(/test.ts-youtube-iframe-api.dominikangerer.com/i) != -1) {
}

// DEV
if (window.location.hostname.search(/localhost/i) != -1 || window.location.hostname.search(/0.0.0.0/i) != -1) {

}